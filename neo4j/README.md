# Neo4j

Neo4j is used to store the model and data lineage for our app. This is just a dummy directory with a dummy Dokcerfile that literally pulls the official neo4j image from Docker Hub. This allows us to deploy it as a container to GCP's Artifect Registry.
