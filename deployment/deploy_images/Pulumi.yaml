name: datadetox-deployment
description: A Python program to build & deploy DataDetox images to GCP
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: python
