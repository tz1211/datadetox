name: datadetox-deploy-k8s
description: A Python program to deploy a containerized service to Google Cloud on Kubernetes clusters
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: python
